{
  "name": "book-store-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "React frontend scaffold for Book Store (reference-only, independent of PHP backend).",
  "type": "module",
  "scripts": {
    "start": "vite --port 3000 --strictPort",
    "dev": "vite --port 3000 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 3000 --strictPort"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}
