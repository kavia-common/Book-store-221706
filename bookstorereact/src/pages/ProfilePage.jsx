import React from "react";

// PUBLIC_INTERFACE
export default function ProfilePage() {
  /** User profile placeholder with TODOs for fetching and editing profile info. */
  return (
    <section>
      <h1>Profile</h1>
      <p>TODO: Fetch profile details from the PHP backend once auth is set up.</p>
    </section>
  );
}
